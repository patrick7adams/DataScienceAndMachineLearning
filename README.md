


General idea:
for each algorithm, have an in-depth explanation of the algorithm, a preprocessing step, modeling step, and evaluation step, showing data along the way
include markdown explaining algorithms also along the way

Algorithms to implement:
supervised:
    Logistic Regression (w/ gradient descent)
    Multilayer Perceptron
    Decision trees

    other:
    ridge/lasso regression
    k-nearest neighbors
    CART
    random forests
    gradient boosting

unsupervised:
    k-means clustering
    hierarchical clustering
    DBSCAN
    PCA
    t-SNE
    autoencoders
Naive Bayes and Probabilistic models

- separate into training/testing data? check if necessary
- find better testing dataset
finish notebooks by early next week, then ask questions about machine learning package in office hours: